import sys
sys.path.append('../data')
import read

class Embedding():
    self.users = {}

    def generate_embeddings():
        lines = dataIterator()
        if lines.author in self.users:
            
        else:
